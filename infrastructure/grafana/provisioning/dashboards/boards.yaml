# # config file version
apiVersion: 1

providers:
 - name: 'LoadLab boards'
   orgId: 1
   folder: ''
   type: file
   allowUiUpdates: true
   updateIntervalSeconds: 60
   options:
     path: /etc/grafana/provisioning/dashboards/json
