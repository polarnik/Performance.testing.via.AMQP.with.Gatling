test {
    jms {
        hostname = "localhost"
        port = 1414
        queueManager = "QM1"
        channel = "DEV.APP.SVRCONN"
        username = "app"

        input {
            name = "DEV.QUEUE.1"
        }

        output {
            name = "DEV.QUEUE.2"
        }
    }
}